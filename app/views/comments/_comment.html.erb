<%= div_for(comment) do %>
  <div class="comments_wrapper clearfix">
    <div class="pull-left">
      <p class="lead comment-body"><%= comment.body %></p>
      <p><small>Submitted <strong><%= time_ago_in_words(comment.created_at) %> ago</strong> by <%= comment.user.username %></small></p>
    </div>

    <div class="pull-right">
      <% if comment.user == current_user -%>
        <%= button_tag 'Edit Comment', class: "btn btn-info btn-sm comments-edit", :data => { :comment_id => comment.id } %>
      <% end %>
      <% if comment.user == current_user -%>
        <%= link_to 'Delete', comment, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-sm" %>
      <% end %>
    </div>
  </div>
<% end %>
