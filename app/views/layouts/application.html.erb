<!DOCTYPE html>
<html>
  <head>
    <title>What Conference?</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag "https://maps.googleapis.com/maps/api/js?v=3.exp&libraries=places&key=#{Nenv.google_browser_api_key}" %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= render "layouts/favicons" %>
    <%= csrf_meta_tags %>
  </head>
  <body class="page">
    <nav class="navbar navbar-default navbar-wc navbar-lg" role="navigation">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse-01">
          <span class="sr-only">Toggle navigation</span>
        </button>
        <%= link_to root_path, :class => "navbar-brand" do %>
          <%= image_tag "logo.png", :class => "brand-image" %>
          What Conference?
        <% end %>

      </div>
      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? -%>
            <li><%= link_to root_path, :class => 'radar-link' do %>
              <span class="nohover"><%= antenna_icon %></span>
              <span class="hover"><%= radar_sweep_icon %></span>
              My Radar
              <% end %>
            </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">Account <b class="caret"></b></a>
              <span class="dropdown-arrow"></span>
              <ul class="dropdown-menu">
                <li><%= link_to 'Settings', account_path %></li>
                <li><%= link_to 'Sign out', destroy_user_session_path, :method => :delete, :class => "user-sign-out" %></li>
              </ul>
            </li>
          <% else -%>
            <li><%= link_to 'Sign in', new_user_session_path %></li>
          <% end -%>
        </ul>

        <% unless content_for(:hide_nav_search) %>
          <form class="navbar-form search-form" action="<%= conferences_path %>" role="search">
            <div class="form-group">
              <div class="input-group">
                <input class="form-control wide conference-search" type="search" name="query" placeholder="Search for conference">
                <span class="input-group-btn">
                  <button type="submit" class="btn"><span class="fui-search"></span></button>
                </span>
              </div>
            </div>
          </form>
        <% end %>
      </div>
    </nav>

    <div class="page-container">
      <% flash.each do |name, msg| %>
        <%= content_tag(:div, msg, class: flash_alert_class(name)) %>
      <% end %>

      <%= yield %>
    </div>

    <%= render "layouts/google_analytics" %>
  </body>
</html>
